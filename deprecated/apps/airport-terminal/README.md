# Deprecated Airport Terminal

This package is retained only as historical reference while Mission moves to the native Airport host.

It is not part of the active workspace, it is not launched by `mission`, and it should not receive new feature work.

The supported operator surface now lives in `apps/airport/native` with the shared frontend in `apps/airport/web`.