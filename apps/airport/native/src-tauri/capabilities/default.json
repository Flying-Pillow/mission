{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Mission Airport native host.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
