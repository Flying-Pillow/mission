This directory is a placeholder mount for the optional /flying-pillow workspace.

If you need the real sibling repository inside the dev container, replace this bind
source in docker/docker-compose.development.yml with your local checkout path.